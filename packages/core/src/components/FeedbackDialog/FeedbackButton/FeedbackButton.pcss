.FeedbackButton {
	/* TODO: Perfect the AppBar spacing. */
	margin-left: 7px !important;
	position: relative !important;
	top: 1px;
}

button.FeedbackButton[type="button"] {
	/* materia-ui hack. */
	padding: 0 !important;
}